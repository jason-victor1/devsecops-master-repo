apiVersion: v1
kind: Service
metadata:
  name: event-ingestion-api
  labels:
    app.kubernetes.io/name: event-ingestion-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: event-ingestion-api
